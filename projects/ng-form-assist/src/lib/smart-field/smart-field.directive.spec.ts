/* import { SmartFieldDirective } from './smart-field.directive';

describe('DynamicMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartFieldDirective();
    expect(directive).toBeTruthy();
  });
}); */
